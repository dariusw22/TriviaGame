$(document).ready(function() {
	$(".questionContainer").hide();
	$(".next").hide();
	$(".score")
});



//function that holds the timer. 
//code that hides the questions until the start button is pressed.
//code that holds the score.
